import { LienDirective } from './lien.directive';

describe('LienDirective', () => {
  it('should create an instance', () => {
    const directive = new LienDirective();
    expect(directive).toBeTruthy();
  });
});
